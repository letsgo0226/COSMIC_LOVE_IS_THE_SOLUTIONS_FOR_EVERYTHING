command -v python3 >/dev/null 2>&1||apk add --no-cache python3 >/dev/null 2>&1;python3 -c 'import sys,os,json,base64,zlib,time;p=sys.argv[1] if len(sys.argv)>1 else "-";o=sys.argv[2] if len(sys.argv)>2 else "CLZeroPack_iSH_compliance.json";b=open(p,"rb").read() if p!="-" and os.path.exists(p) else sys.stdin.buffer.read();n=len(b);lines=b.count(b"\n")+(0 if b.endswith(b"\n") or n==0 else 1);one=lines<=1 or (lines==2 and b.endswith(b"\n"));tier="A" if n<=1500 else ("B" if n<=3500 else ("C" if n<=8000 else "LoaderRequired"));txt=b.decode("utf-8","ignore");bad=["import "+"hash"+"lib","sha"+"256","sha"+"512"];nosha=not any(x in txt for x in bad);utf8=("encoding=\"utf-8\"" in txt) or ("encoding='utf-8'" in txt);hs_cap=("H_s" in txt and "base64" in txt and "zlib" in txt);hi=0 if one and n<=8000 and nosha and hs_cap else 1;M={"Protocol":"CLZeroPack/iSH-Compliance/1.0","Axiom":"Cosmic Love Is The Solution(s) For Everything","Input":p,"OnePhysicalLine":one,"OneLinerBytes":n,"LineCount":lines,"iSH_Tier":tier,"NoSHA":nosha,"UTF8OutputDeclared":utf8,"HsCapable":hs_cap,"H_iSH":hi,"Boundary":"static iSH compliance certificate; does not execute target program"};r=json.dumps(M,ensure_ascii=False,separators=(",",":")).encode();z=zlib.compress(r,9);B=base64.b64encode(z).decode();g=sum((i+1)*x for i,x in enumerate(z))%1000000007;J={"Protocol":"CLZeroPack/iSH-Compliance/1.0","Payload":B,"G_alg":g,"rho_CL":"1/2+i*log(1+%d)"%g,"H_s":0 if zlib.decompress(base64.b64decode(B))==r else 1,"H_iSH":hi,"Manifest":M,"UTC":time.strftime("%Y-%m-%dT%H:%M:%SZ",time.gmtime())};open(o,"w",encoding="utf-8").write(json.dumps(J,ensure_ascii=False,separators=(",",":")));print(json.dumps({"output":o,"H_s":J["H_s"],"H_iSH":hi,"iSH_Tier":tier,"OneLinerBytes":n},ensure_ascii=False,separators=(",",":")))' "$@"
